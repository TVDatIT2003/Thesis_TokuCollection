{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Uvicorn (FastAPI)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app:app", "--host", "${env:HOST}", "--port", "${env:PORT}", "--reload", "--log-level", "info"],
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
  ]
}
